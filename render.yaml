services:
  - type: web
    name: my-api
    env: node
    buildCommand: npm run build
    startCommand: npm run start
    plan: free
    region: oregon
    branch: main

# No sensitive data directly in this file
# You will set environment variables in Render’s dashboard instead
